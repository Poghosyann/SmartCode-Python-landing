/**
 * Analytics configuration — fill in your IDs to enable tracking.
 * Leave empty strings to keep analytics disabled (no scripts loaded).
 */
window.SCA_CONFIG = {
    analytics: {
        ga4MeasurementId: '', // e.g. 'G-XXXXXXXXXX'
        metaPixelId: '',      // e.g. '123456789012345'
    },
};
